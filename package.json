{
  "name": "@aptre/bldr-saucer",
  "version": "0.3.1",
  "description": "Native webview bridge for Bldr using Saucer",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/aperturerobotics/bldr-saucer.git"
  },
  "keywords": [
    "webview",
    "saucer",
    "native",
    "bldr"
  ],
  "author": "Aptre",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/aperturerobotics/bldr-saucer/issues"
  },
  "homepage": "https://github.com/aperturerobotics/bldr-saucer#readme",
  "scripts": {
    "postinstall": "node install.js",
    "gen": "bun run go:aptre -- generate",
    "gen:force": "bun run go:aptre -- generate --force",
    "go:aptre": "go run -mod=mod github.com/aperturerobotics/common/cmd/aptre",
    "release": "npm run release:version && npm run release:sync && npm run release:commit",
    "release:minor": "npm run release:version:minor && npm run release:sync && npm run release:commit",
    "release:version": "npm version patch --no-git-tag-version",
    "release:version:minor": "npm version minor --no-git-tag-version",
    "release:sync": "node -e \"const v=require('./package.json').version;const fs=require('fs');for(const p of['darwin-arm64','darwin-x64','linux-x64','linux-arm64','win32-x64']){const f='npm/'+p+'/package.json';const j=JSON.parse(fs.readFileSync(f));j.version=v;fs.writeFileSync(f,JSON.stringify(j,null,2)+'\\n');}const m=JSON.parse(fs.readFileSync('package.json'));for(const k of Object.keys(m.optionalDependencies))m.optionalDependencies[k]=v;fs.writeFileSync('package.json',JSON.stringify(m,null,2)+'\\n');\"",
    "release:commit": "git reset && git add package.json npm/*/package.json && git commit -s -m \"release: v$(node -p \"require('./package.json').version\")\" && git tag v$(node -p \"require('./package.json').version\")",
    "release:publish": "git push && git push --tags"
  },
  "optionalDependencies": {
    "@aptre/bldr-saucer-darwin-arm64": "0.3.1",
    "@aptre/bldr-saucer-darwin-x64": "0.3.1",
    "@aptre/bldr-saucer-linux-x64": "0.3.1",
    "@aptre/bldr-saucer-linux-arm64": "0.3.1",
    "@aptre/bldr-saucer-win32-x64": "0.3.1"
  },
  "files": [
    "index.js",
    "install.js",
    "src/**/*",
    "CMakeLists.txt"
  ],
  "devDependencies": {
    "@aptre/common": "^0.30.3"
  },
  "dependencies": {
    "@aptre/protobuf-es-lite": "^1.0.0"
  }
}
